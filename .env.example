# Neon Postgres 연결 문자열 (Vercel에 이미 연결돼 있으면 자동 주입됩니다)
DATABASE_URL=

# 계정 목록. "이름:PIN" 을 쉼표로 구분합니다. PIN은 원하는 값으로 바꾸세요.
ACCOUNTS=남편:1234,아내:5678

# 세션 쿠키 서명 키. 16자 이상의 아무 랜덤 문자열.
# 만들기: openssl rand -base64 32
AUTH_SECRET=
